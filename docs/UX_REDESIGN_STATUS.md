# QuatHub UX Redesign — Status

Обновлено: 2026-09-17.

## Текущий этап

**UX-1…UX-8 завершены** (локально). Публичный стенд не обновлялся в этом прогоне.

## Журнал

| Этап | Статус | Результат |
|---|---|---|
| UX-1 Аудит | done | `UX_REDESIGN_BRIEF.md`, `UX_REDESIGN_PLAN.md` |
| UX-2 UI kit | done | токены `#D9FE54`/`#E2E2E2`, `src/components/ui/*`, `/ui` |
| UX-3 Workspace + nav | done | шапка Каталог/Мои сметы/Аккаунт; каталог∥смета; `/` → catalog |
| UX-4 Add + qty | done | «Добавить» в списке, guest merge, stepper sync, без редиректа |
| UX-5 Estimate/forms | done | упрощённый `EstimateWorkspace`, download menu, login/projects стиль |
| UX-6 Toast | done | графитовый ToastProvider в layout; демо на `/ui` |
| UX-7 Cleanup | done | future-пункты убраны из бара; роли в меню аккаунта |
| UX-8 Acceptance | done | unit 74/74; e2e 5/5; ручная проверка Add на localhost |

## Что работает (проверено localhost)

- Каталог + панель сметы на одном экране; Add → stepper и строка сметы без смены URL.
- Гостевой merge одинаковых позиций; локальный итог.
- Шапка без Заявки/Поставщик/Возможности в основном ряду.
- UI kit `/ui` со всеми базовыми состояниями.
- Unit-тесты: 74 passed.

## Остаточные ограничения

- Auth-пользователь на каталоге пишет в **локальный** черновик до «Сохранить в аккаунт» (`/app/import-draft`).
- Полная визуальная миграция всех admin/supplier форм — частично (токены через alias, не все кнопки переведены).
- React Bits: эффект toast реализован совместимо без внешней зависимости (лицензия/API проверены концептуально; отдельный пакет не ставился).
- Атрибут poles: подпись «Число полюсов» в UI + seed; существующая БД стенда может ещё хранить старое `nameRu` до reseeds/`UPDATE`.

## Не в scope

Production infra (этап 9), нормативный движок, платежи.
